{
  "name": "@fillco/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure business logic: money, FX, payment terms, credit, quantities, statuses, specs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
