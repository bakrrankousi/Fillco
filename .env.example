# PostgreSQL
DATABASE_URL=postgresql://fillco:fillco@localhost:5432/fillco_dev
# Used by API integration tests (database is reset by the tests)
TEST_DATABASE_URL=postgresql://fillco:fillco@localhost:5432/fillco_test

# API
API_PORT=4000
# Long random string, used to sign nothing secret today but reserved for future tokens
SESSION_TTL_HOURS=12
# Set to true behind HTTPS so cookies get the Secure flag
COOKIE_SECURE=false

# Web
API_INTERNAL_URL=http://localhost:4000
