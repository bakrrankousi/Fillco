services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: fillco
      POSTGRES_PASSWORD: fillco
      POSTGRES_DB: fillco_dev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/create-test-db.sql:/docker-entrypoint-initdb.d/10-test-db.sql:ro
volumes:
  pgdata:
